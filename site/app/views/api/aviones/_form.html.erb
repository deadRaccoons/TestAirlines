<%= form_for(@api_avione) do |f| %>
  <% if @api_avione.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_avione.errors.count, "error") %> prohibited this api_avione from being saved:</h2>

      <ul>
      <% @api_avione.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
